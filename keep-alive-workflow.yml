# ============================================================
# এই ফাইলটি GitHub-এ তৈরি করতে হবে:
# 
# ১. https://github.com/emonhmamun/keep-alive-pinger/new/main/.github/workflows
# ২. ফাইলের নাম: keep-alive.yml
# ৩. নিচের সম্পূর্ণ কোড কপি করে পেস্ট করুন
# ৪. Commit new file ক্লিক করুন
# ============================================================

name: 24/7 Keep-Alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Ping
        env:
          PUBLIC_URL: ${{ secrets.PUBLIC_URL }}
        run: |
          if [ -z "$PUBLIC_URL" ]; then
            echo "PUBLIC_URL secret not set"
            exit 0
          fi
          RESPONSE=$(curl -s -m 10 -o /dev/null -w "%{http_code}" "$PUBLIC_URL/api/system-health" 2>/dev/null || echo "000")
          if [ "$RESPONSE" != "000" ]; then
            echo "✅ Service ALIVE (HTTP $RESPONSE)"
          else
            echo "❌ Service DOWN"
          fi
          curl -s -m 10 -o /dev/null "$PUBLIC_URL" 2>/dev/null || true
          echo "✅ Traffic sent"
